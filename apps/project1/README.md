# Project 1

Project 1 contains a FastAPI backend and a Streamlit frontend that share the workspace environment and any common libraries under `src/shared`.

## Layout

- `backend/` for FastAPI entrypoints and API logic
- `frontend/` for Streamlit UI code
- `tests/` for project-specific tests
