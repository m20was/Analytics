# DML — Data Manipulation Language

Place data manipulation scripts here:
- `INSERT`, `UPDATE`, `DELETE`, `MERGE`

Subdirectories:
- `seeds/` — static reference / lookup data loaded once
- `migrations/` — one-time data transformation or backfill scripts
